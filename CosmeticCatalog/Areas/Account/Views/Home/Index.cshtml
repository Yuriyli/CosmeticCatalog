@model UserVM

@{
    ViewData["Title"] = "Account";
}

<h3>@Model.Name</h3>
<h3>@Model.Email</h3>
@if (Model.Roles.Count > 0)
{
    string roles = "Roles: ";
    @foreach (var r in Model.Roles)
    {
        roles += r + " ";
    }
    <h3>@roles</h3>
}
